let numCircles = 5; // Number of circles
let circleSize = 10; // Size of each circle
let circleSpacing = 20; // Spacing between circles
let circleSpeedMultiplier = 0.05; // Speed multiplier for circle movement

function setup() {
  createCanvas(100, 100);
}

function draw() {
  background(255);

  //For loop which goes through each circle
    // Initial x position at the left side of the canvas
    // Calculate y position based on the index of the circle
    // Calculate the position of the circle based on the x-position of the mouse and the index
    // Update the x-position of the circle with the newly calcualted position 
    //Draw the circle at the updated speed
}
