
function drawEllipse(posX, posY, w, h, r, g, b){
    fill(r, g, b);
    ellipse(posX, posY, w, h);
}

//Create the drawEllipse function
    //Specify the fill of the ellipse
    //Create the ellipse

function setup() {
    createCanvas(100,100);
}

function draw() {
    background(255);
    
    //Create ellipse using the draw ellipse function
    //Create ellipse using the draw ellipse function
    //Create ellipse using the draw ellipse function
}

