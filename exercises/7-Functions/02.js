//Create getArea() function

function setup() {
    createCanvas(100,100);

    // Use the ellipse to visualise the value obtained from the getArea function
    // ellipse(width/2, height/2, 20, getArea(....));
}

function draw() {
    background(255);
}

