class Shape {
    constructor(x, y, wid, len) {
      this.x = x;
      this.y = y;
      this.wid = wid;
      this.len = len;
    }
}

function setup() {
    createCanvas(100,100);
}

function draw() {
    background(255);

    //Create and object of the Shape class
    //Create and object of the Shape class

    //Draw a rectangle using the values of the shape class
    //Draw a rectangle using the values of the shape class
}

