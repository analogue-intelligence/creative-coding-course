// Draw using the mouse, and also work with mousePressed() and keyPressed() events

function setup() {

  createCanvas(100, 100);
  background(0); // Background is specified here so as to be able to draw on canvas

}

function draw() {

}

// Implement mousePressed function. This function will be called automatically everytime the user pressed the mouse's left button
// Check for more details: https://p5js.org/reference/#/p5/mousePressed
// Every time the mouse is pressed a circle is drawn on the current mouse coordinates

// Implement keyPressed function. This function will be called automatically everytime the user presses a keyboard key
// Check for more details: https://p5js.org/reference/#/p5/keyPressed
// When a key is pressed the background becomes white again - refreshing the screen